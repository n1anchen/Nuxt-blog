# GitHub API Token for repository card component
# 从 https://github.com/settings/tokens 获取 (仅需 public_repo 权限)
# 不需要将此文件提交到版本控制，使用本地 .env 文件
# 也可以不创建 .env 文件，从 NUXT_GITHUB_TOKEN 或 GITHUB_TOKEN 环境变量读取
GITHUB_TOKEN=your_github_token_here
