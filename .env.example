# GitHub API Token for repository card component
# 从 https://github.com/settings/tokens 获取 (需要 public_repo 权限)
# 不需要将此文件提交到版本控制，使用本地 .env 文件
GITHUB_TOKEN=your_github_token_here

# 可选：预加载缓存（构建前运行）
# npx tsx scripts/preload-github-cache.ts
